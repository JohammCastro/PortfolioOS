import React, { useEffect } from 'react';
import PropTypes from 'prop-types';

const Welcome = ({ }) => {
	useEffect(() => {
		
	}, [])

	return <div style={{fontSize:30}}>Welcome!</div>;
};

Welcome.propTypes = {};

export default Welcome;